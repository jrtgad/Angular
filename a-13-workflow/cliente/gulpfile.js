/** Fichero de tareas asíncronas */
var gulp = require('gulp');
/* requerimientos de plugins de gulp */
// https://www.npmjs.com/package/gulp-bundle-assets


gulp.task('createBundle', [], function () {
	
});

/** tarea por defecto: se lanza con el comando gulp */
/** depende de la tarea  crea */
gulp.task('default', ['createBundle']);